2.3 공정성 측정
JFI 식에서 xi는 각 사용자가 받는 자원의 양이라고 했는데, 정확히 뭔지? 
단순히 토큰 또는 시간이라고 하기엔 Priority와 WFQ에서는 가중치가 서로 다른 상황이기 때문에 xi = 실제 사용한 자원의 양(토큰 또는 시간) / 그 테넌트에게 약속된 가중치 (Weight)으로 해야하는 거 아닌가?
그런데 3.4 핵심 기술 특징의 공정성 측정 부분에서는 ‘WFQ처럼 등급별로 의도적인 차등 서비스를 제공하는 시스템에서는 전체 테넌트 간 JFI가 낮게 나오는 것이 오히려 정상이다.’라고 되어있는 것을 보면 가중치를 고려하지 않고 동일한 JFI 계산식을 사용하는 거 같은데….
모든 알고리즘에 가중치를 배제시키고 JFI를 계산하는 쪽으로? 아니면 알고리즘 별로 JFI 식을 다르게 하는 쪽으로?
또한 시스템 수준에서의 JFI와 테넌트 수준에서의 JFI 계산 식의 차이? xi의 계산의 차이?


3.3.2 Priority
요청의 우선순위는 API 계층에서 테넌트 등급과 요청 헤더의 긴급도 필드를 조합하여 결정한다.

3.3.4 WFQ
테넌트 등급별 가중치에 비례하여 자원을 분배하는 알고리즘이다.

-> 그럼 WFQ에서의 가중치는 요청 헤더의 긴급도 필드를 보지 않고 테넌트 등급만 보는건가? 만약 요청 헤더 긴급도도 본다면 Priority와 WFQ의 등급 계산 결과는 동일한건가? 또한 VFT 계산의 분모에 등급 + 긴급도가 들어가는건지?

3.2 시스템 아키텍처의 스케줄러 엔진 내용이랑 3.4 핵심 기술 특징의 실시간 알고리즘 교체 내용이랑 같은 문장이 겹침. 둘 중 한 섹션에서만 자세히 설명해도 될 듯.

4.1 실험 설계
‘3.6절의 구현 환경에서 다음과 같은 조건으로 실험을 수행할 계획이다’라고 되어있는데 3.6절은 없음
